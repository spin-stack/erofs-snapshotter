name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Run golangci-lint
        uses:  golangci/golangci-lint-action@v9
        with:
          version: latest

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Download Go modules
        run: go mod download

      - name: Cache erofs-utils
        id: cache-erofs
        uses: actions/cache@v4
        with:
          path: ~/.local/erofs-utils
          key: erofs-utils-1.8.10-amd64-v1

      - name: Build erofs-utils in Docker
        if: steps.cache-erofs.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/.local/erofs-utils
          docker run --rm \
            -v ${{ github.workspace }}:/workspace \
            -v ~/.local/erofs-utils:/output \
            -w /workspace \
            ubuntu:25.04 bash -c '
              set -ex
              chmod +x ./scripts/build-erofs-utils.sh
              ./scripts/build-erofs-utils.sh /output
            '

      - name: Install erofs-utils
        run: |
          sudo cp ~/.local/erofs-utils/bin/* /usr/local/bin/
          mkfs.erofs --version

      - name: Load EROFS kernel module
        run: sudo modprobe erofs || true

      - name: Setup udev rules for loop devices
        run: |
          # Skip SCSI detection/warnings and reduce CPU overhead for loop devices
          sudo tee /etc/udev/rules.d/49-skip-loop-scsi.rules << 'EOF'
          SUBSYSTEM=="block", KERNEL=="loop*", ENV{ID_SERIAL}="loop", ENV{ID_SCSI}="0", ENV{ID_SCSI_INQUIRY}="0"
          SUBSYSTEM=="block", KERNEL=="loop*", ENV{UDISKS_IGNORE}="1"
          SUBSYSTEM=="block", KERNEL=="loop*", ENV{SYSTEMD_READY}="0", OPTIONS+="nowatch"
          EOF
          sudo udevadm control --reload-rules

      - name: Run tests as root
        run: |
          # Run in private mount namespace to avoid systemd mount unit overhead
          sudo unshare -m sh -c "
            mount --make-rprivate /
            env PATH=$PATH GOPATH=$GOPATH GOCACHE=$GOCACHE GOMODCACHE=$GOMODCACHE \
              go test -v -race -coverprofile=coverage.txt -covermode=atomic ./... -test.root
          "

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.txt
          fail_ci_if_error: false

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux]
        goarch: [amd64, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Download Go modules
        run: go mod download

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          go build -trimpath -ldflags "-s -w" -o nexuserofs-snapshotter-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/nexuserofs-snapshotter

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nexuserofs-snapshotter-${{ matrix.goos }}-${{ matrix.goarch }}
          path: nexuserofs-snapshotter-${{ matrix.goos }}-${{ matrix.goarch }}

  # Build and optionally push integration test image
  integration-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image: ${{ steps.meta.outputs.tags }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/integration
          tags: |
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=sha,prefix=,format=short

      - name: Build and push integration image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.integration
          platforms: linux/amd64
          push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          load: ${{ github.event_name != 'push' || github.ref != 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  integration-test:
    runs-on: ubuntu-latest
    needs: [lint, test, integration-image]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run integration tests
        run: |
          sudo modprobe erofs

          # Use the image from the previous job
          IMAGE="${{ needs.integration-image.outputs.image }}"
          # Extract the first tag (in case multiple were generated)
          IMAGE=$(echo "$IMAGE" | head -n1)

          echo "Using integration image: $IMAGE"

          docker run --privileged --rm \
            -v /dev:/dev \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            --tmpfs /tmp:exec \
            --tmpfs /run:exec \
            --tmpfs /var/lib/containerd-test:exec \
            --tmpfs /var/lib/nexuserofs-snapshotter:exec \
            "$IMAGE"

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-logs
          path: /tmp/integration-logs/
          retention-days: 5
